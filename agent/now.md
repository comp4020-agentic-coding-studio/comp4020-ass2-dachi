# Hand-off --- assignment 2 (Doorology), deepen run, 69.0h to cutoff

## State

`comp4020-ass2-dachi` remains structurally complete, ~59% of the week
elapsed (window opened 2026-09-14T12:00, due 2026-09-21T12:00). `git status`
clean, one commit this run
([`5d58223`](https://github.com/comp4020-agentic-coding-studio/comp4020-ass2-dachi/commit/5d58223)),
already pushed --- a `CLAUDE.md` entry logging the one deepening pass this
run ran, no source/content fixes needed. `pnpm check` green at the top of
this run (32 pages, 0 a11y violations, 0 broken links, 6/6 tests).

Picked up the one item the prior hand-off queued: a dedicated throughline
check, reading all four assessment briefs (door-audit, redesign-proposal,
prototype-crit, field-guide) side by side against the brief's own
model-course criterion (one idea carried the whole semester, not four
assignments sharing a topic). It holds --- audit's signifier/affordance
vocabulary becomes redesign's access/trust lenses, prototype tests whether
the redesign's claim survives a stranger, field guide asks whether the
categories generalise past the doors that produced them; every stage asks
the same question (does this door tell the truth about what it does) at
increasing grain. Also reconsidered the course's own positioning
("Doorology" vs. the model courses' more argumentative titles) and
concluded the home page's opening line already carries the point of view
the course-code title doesn't --- not worth a rename this late for no
added claim.

No edit resulted from either half of this pass. That's three consecutive
dry runs now (this run's content/throughline pass, plus the prior run's
four sensor angles, plus the run before that's five) --- weaker than
crit5's five-straight-dry threshold for calling the well fully dry, but
combined with ~59% of the week elapsed this is close to the point where
past crit precedent (crit5: 61%+ elapsed, 5 dry runs) switched to
light-touch re-verification rather than inventing further passes.

## Next action

Doctrine step 4 reserves `PROCESS.md` (and any other finishing step) for
the run the prompt actually calls last --- this run's prompt didn't, so it
stays the unfilled template, deliberately. Don't draft it yet, even though
elapsed-time-and-dry-sensors arithmetic is now close to crit5's threshold:
that threshold is about when a *reflection* becomes safe to draft early
(crit series only, since crits get `reflections/crit-<n>.md`), not
`PROCESS.md`, which the doctrine ties strictly to "the run called last,"
full stop --- an assessment has no early-draftable reflection at all.

Next run: if the prompt still hasn't called it last, rerun `pnpm check` as
a light-touch confirm rather than manufacturing a fourth speculative
content/design pass. If sensors stay dry and a subsequent hand-off still
can't find a new lever, that's fine --- it means the site is settled with
runway left, not that something's missing. When the prompt does call a run
last: work doctrine's finishing steps in order (verify live, draft
`PROCESS.md` 400--600 words citing real commits per the brief's own rubric
--- legibility of process is 45% of the mark, the largest single
criterion --- commit, push, confirm the live GitHub Pages URL serves the
pushed commit).
